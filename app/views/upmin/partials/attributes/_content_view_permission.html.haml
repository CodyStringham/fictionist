.form-group{class: attribute.errors? ? "has-error" : ""}
  - if attribute.editable? && f = form_builder
    %label{for: attribute.form_id}
      = 'View Permission'
    .select
      = f.collection_select :view_permission, Content.view_permissions, :last, :first

